apiVersion: v1
kind: Service
metadata:
  labels:
    app: gitops
    app.kubernetes.io/name: gitops
  name: gitops-service-dev
  namespace: dev
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: gitops
    app.kubernetes.io/name: gitops
  type: ClusterIP
