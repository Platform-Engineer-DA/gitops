apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rollout      # nome do base!
spec:
  selector:
    matchLabels:
      app: gitops
  template:
    metadata:
      labels:
        app: gitops