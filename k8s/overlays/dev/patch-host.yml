apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress        # nome do base
spec:
  rules:
    - host: gitops-dev.codeia-ti.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stable
                port:
                  number: 80